# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
  branches:
    include:
      - master
      - 202???
  paths:
    exclude:
      - .github

pr:
  branches:
    include:
      - master
      - 202???
  paths:
    exclude:
      - .github

name: $(TeamProject)_$(Build.DefinitionName)_$(SourceBranchName)_$(Date:yyyyMMdd)$(Rev:.r)

resources:
  repositories:
  - repository: sonic-mgmt
    type: github
    name: arthur-cog-sonic/sonic-mgmt
    ref: master
    endpoint: arthur-cog-sonic
  - repository: buildimage
    type: github
    name: arthur-cog-sonic/sonic-buildimage
    endpoint: arthur-cog-sonic
    ref: master

parameters:
- name: TIMEOUT_IN_MINUTES_PR_TEST
  type: number
  default: 480

- name: CHECKOUT_SONIC_MGMT
  type: boolean
  default: true

variables:
- template: .azure-pipelines/azure-pipelines-repd-build-variables.yml@buildimage
- template: .azure-pipelines/template-variables.yml@buildimage
- name: CACHE_MODE
  value: wcache
- name: ENABLE_FIPS
  value: y
- name: BUILD_BRANCH
  ${{ if eq(variables['Build.Reason'], 'PullRequest') }}:
    value: $(System.PullRequest.TargetBranch)
  ${{ else }}:
    value: $(Build.SourceBranchName)

stages:
- stage: BuildVS
  pool: sonic-build
  jobs:
  - template: .azure-pipelines/azure-pipelines-build.yml
    parameters:
      buildOptions: 'USERNAME=admin SONIC_BUILD_JOBS=$(nproc) BUILD_MULTIASIC_KVM=y INCLUDE_DHCP_SERVER=y ${{ variables.VERSION_CONTROL_OPTIONS }}'
      jobGroups:
      - name: vs

# Platform builds commented out - no DUTs available for testing
# - stage: Build
#   pool: sonic-build
#   dependsOn: []
#   jobs:
#   - template: .azure-pipelines/azure-pipelines-build.yml
#     parameters:
#       buildOptions: 'USERNAME=admin SONIC_BUILD_JOBS=$(nproc) ${{ variables.VERSION_CONTROL_OPTIONS }}'
#       jobGroups:
#       - name: broadcom
#         variables:
#           swi_image: yes
#           docker_syncd_rpc_image: yes
#           platform_rpc: brcm
#           INCLUDE_RESTAPI: y
#       - name: mellanox
#         variables:
#           dbg_image: yes
#           docker_syncd_rpc_image: yes
#           platform_rpc: mlnx
# ARM builds commented out - no hardware available
#       - name: marvell-prestera-arm64
#         pool: sonic-build
#         variables:
#            PLATFORM_NAME: marvell-prestera
#            PLATFORM_ARCH: arm64
#       - name: marvell-prestera-armhf
#         pool: sonic-build
#         timeoutInMinutes: 1200
#         variables:
#           PLATFORM_NAME: marvell-prestera
#           PLATFORM_ARCH: armhf
#           INCLUDE_RESTAPI: y

# Restored: Test stage re-enabled with local KVM execution (pr_test_local.yml)
# replacing Microsoft's Elastictest service which is not available in Cisco-SONiC-PoC.
# The SONiC-Elastictest variable group dependency has been removed.
- stage: Test
  pool: sonic-build
  dependsOn: BuildVS
  condition: and(succeeded(), and(ne(stageDependencies.BuildVS.outputs['vs.SetVar.SKIP_VSTEST'], 'YES'), in(dependencies.BuildVS.result, 'Succeeded', 'SucceededWithIssues')))
  variables:
  # Removed: group: SONiC-Elastictest (not available in Cisco-SONiC-PoC)
  - name: inventory
    value: veos_vtb
  - name: testbed_file
    value: vtestbed.csv

  jobs:
  # debug_variables and vstest jobs removed — they were previously disabled
  # (condition: false) and are not needed for local KVM execution.

  # KVM based PR test jobs — restored from comments, using pr_test_local.yml
  # instead of pr_test_template.yml to run tests locally on sonic-build-01
  # rather than submitting to Elastictest.
  - template: .azure-pipelines/pr_test_local.yml@sonic-mgmt
    parameters:
      CHECKOUT_SONIC_MGMT: ${{ parameters.CHECKOUT_SONIC_MGMT }}
      TIMEOUT_IN_MINUTES_PR_TEST: ${{ parameters.TIMEOUT_IN_MINUTES_PR_TEST }}
